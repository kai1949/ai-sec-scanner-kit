Run ai-sec-scanner-kit workflow for this repository.

Use `.codex/agents/ai-sec-orchestrator.md` as the coordinator prompt and execute:
1. architecture
2. dataflow-scanner + security-auditor
3. verification
4. reporter

Write all results under `scan-results/`.
